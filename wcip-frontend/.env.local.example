# Public base URL the browser uses to reach the API.
# In dev this points at the Next rewrite proxy (see next.config.mjs).
NEXT_PUBLIC_API_BASE=/backend/api/v1

# Server-side internal URL for the rewrite proxy.
BACKEND_INTERNAL_URL=http://localhost:8000
